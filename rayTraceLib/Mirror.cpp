#include "Mirror.h"

png::rgb_pixel Mirror::getColor(const Vector3D e, const Vector3D intersect, const Vector3D normal, const std::vector<Light*> lights, const std::vector<int> hitLights) const
{

	return png::rgb_pixel(0,0,0);

}